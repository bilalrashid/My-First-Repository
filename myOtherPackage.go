//Package greetings merge 2 strings
package greetings

//Merge is the funtion which merges 2 strings and return a single string
func Merge(str1 string, str2 string) string {
	return str1 + "" + str2
}
